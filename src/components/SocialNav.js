import { AppBar, Typography } from "@mui/material";

export default function SocialNav() {
  return (
    <AppBar position="static" sx={{ backgroundColor: "#c7ab59" }}>
      <Typography>Testing</Typography>
    </AppBar>
  );
}
